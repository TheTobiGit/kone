/** Which list the inbox is showing. Two places, not two filters: the archive is
 *  a disjoint view of the same table rather than a subset of the live list. */
export type InboxViewId = "inbox" | "archived";
